export const metadata = {
    title: "Convert Images to WebP | WebPify",
    description: "Convert any image to WebP under 50KB without losing visible quality.",
};

export default function ToolLayout({ children }) {
    return children;
}
